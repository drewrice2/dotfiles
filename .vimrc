set backspace=2                  " backspace to erase
set number                       " line numbers

set tabstop=4                    " num spaces when \t is pressed
set shiftwidth=4                 " num spaces inserted for indentation
set expandtab                    " converts tab to space

